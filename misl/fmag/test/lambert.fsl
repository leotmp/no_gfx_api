fn saturate(x: f32) -> f32 {
	if x < 0 { return 0 }
	if x > 1 { return 1 }
	return x
}

fn main(n: f32x3, l: f32x3, albedo: f32x3, ambient: f32) -> f32x3 {
	let ndotl = n.x * l.x + n.y * l.y + n.z * l.z
	let wrap = saturate(ndotl * 0.5 + 0.5)
	return albedo * (ambient + wrap)
}

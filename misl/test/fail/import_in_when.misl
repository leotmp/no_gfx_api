// Expect: import declarations must be at file scope
when true {
	import "core:debug"
}

vs :: proc "vertex"(vid: u32 | SV_Vertex) -> (pos: [4]f32 | SV_Position) {
	_ = vid
	return [4]f32{0, 0, 0, 1}
}

// Expect: assignment mismatch: 3 variables for 2 values
vec4 :: [4]f32

Data :: struct #align(16) {
	v: f32,
}

VS_Out :: struct {
	pos: [4]f32 | SV_Position,
}

split :: proc(x: f32) -> (a: f32, b: f32) {
	return x, x
}

vs :: proc "vertex"(
	data: ^Data | SV_Data,
	vertex_id: u32 | SV_Vertex,
) -> (out: VS_Out) {
	a, b, c := split(1.0)
	_ = a
	_ = b
	_ = c
	out.pos = vec4(0, 0, 0, 1)
	return out
}

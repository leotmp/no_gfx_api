import "../import_deps/config_dup_chain_lib.misl"

Y :: #config(CHAIN_KEY, 2)

vs :: proc "vertex"(
	vid: u32 | SV_Vertex,
) -> (out: [4]f32 | SV_Position) {
	_ = vid
	_ = Y
	out = [4]f32{0, 0, 0, 1}
	return out
}
